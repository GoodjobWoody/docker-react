language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t woodygoodjob/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true woodygoodjob/docker-react npm run test -- --coverage